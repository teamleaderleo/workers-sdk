diff --git a/packages/containers-shared/src/login.ts b/packages/containers-shared/src/login.ts
--- a/packages/containers-shared/src/login.ts
+++ b/packages/containers-shared/src/login.ts
@@ -3,8 +3,11 @@ import { spawn } from "node:child_process";
 import { UserError } from "@cloudflare/workers-utils";
 import { ImageRegistriesService, ImageRegistryPermissions } from "./client";
 import { OpenAPI } from "./client/core/OpenAPI";
+import { request } from "./client/core/request";
+import type { AccountRegistryToken } from "./client/models/AccountRegistryToken";
+import type { ImageRegistryCredentialsConfiguration } from "./client/models/ImageRegistryCredentialsConfiguration";
+import type { OpenAPIConfig } from "./client/core/OpenAPI";
+
+export interface RegistryCredentialsClient {
+	generateImageRegistryCredentials(
+		domain: string,
+		requestBody: ImageRegistryCredentialsConfiguration
+	): Promise<AccountRegistryToken>;
+}
+
+export function createRegistryCredentialsClient(options: {
+	accountId: string;
+	apiToken: string;
+	apiBase?: string;
+}): RegistryCredentialsClient {
+	const config: OpenAPIConfig = {
+		...OpenAPI,
+		BASE: `${options.apiBase ?? "https://api.cloudflare.com/client/v4"}/accounts/${options.accountId}/containers`,
+		CREDENTIALS: "omit",
+		HEADERS: { Authorization: `Bearer ${options.apiToken}` },
+	};
+	return {
+		generateImageRegistryCredentials(domain, requestBody) {
+			return request<AccountRegistryToken>(config, {
+				method: "POST",
+				url: "/registries/{domain}/credentials",
+				path: { domain },
+				body: requestBody,
+				mediaType: "application/json",
+				errors: {
+					400: "Bad Request",
+					404: "The image registry does not exist",
+					409: "The registry is public",
+					500: "Internal Error",
+				},
+			});
+		},
+	};
+}
@@ -28,7 +51,8 @@ export async function dockerLoginImageRegistry(
 	pathToDocker: string,
-	domain: string
+	domain: string,
+	client: RegistryCredentialsClient = ImageRegistriesService
 ) {
@@ -35,7 +59,7 @@ export async function dockerLoginImageRegistry(
-		await ImageRegistriesService.generateImageRegistryCredentials(domain, {
+		await client.generateImageRegistryCredentials(domain, {
 			expiration_minutes: expirationMinutes,
@@
 diff --git a/packages/containers-shared/src/images.ts b/packages/containers-shared/src/images.ts
--- a/packages/containers-shared/src/images.ts
+++ b/packages/containers-shared/src/images.ts
@@ -4,7 +4,7 @@ import { buildImage } from "./build";
-import { dockerLoginImageRegistry } from "./login";
+import { dockerLoginImageRegistry, type RegistryCredentialsClient } from "./login";
@@ -49,7 +49,8 @@ export async function pullImage(
 	dockerPath: string,
 	options: Exclude<ContainerDevOptions, DockerfileConfig>,
-	logger: WranglerLogger | ViteLogger
+	logger: WranglerLogger | ViteLogger,
+	registryCredentialsClient?: RegistryCredentialsClient
 ) {
@@ -56,7 +57,11 @@ export async function pullImage(
 	const isExternalRegistry = domain !== getCloudflareContainerRegistry();
 	try {
-		await dockerLoginImageRegistry(dockerPath, domain);
+		if (!registryCredentialsClient) {
+			throw new Error("No operation-scoped registry credentials client configured");
+		}
+		await dockerLoginImageRegistry(dockerPath, domain, registryCredentialsClient);
 	} catch (e) {
@@ -114,6 +119,7 @@ export async function prepareContainerImagesForDev(args: {
 	logger: WranglerLogger | ViteLogger;
+	registryCredentialsClient?: RegistryCredentialsClient;
 }): Promise<void> {
@@ -155,7 +161,12 @@ export async function prepareContainerImagesForDev(args: {
-			const pull = await pullImage(dockerPath, options, args.logger);
+			const pull = await pullImage(
+				dockerPath,
+				options,
+				args.logger,
+				args.registryCredentialsClient
+			);
@@
 diff --git a/packages/vite-plugin-cloudflare/src/plugins/dev.ts b/packages/vite-plugin-cloudflare/src/plugins/dev.ts
--- a/packages/vite-plugin-cloudflare/src/plugins/dev.ts
+++ b/packages/vite-plugin-cloudflare/src/plugins/dev.ts
@@ -1,6 +1,7 @@
 import {
-	configureOpenAPIForContainerPull,
+	createRegistryCredentialsClient,
@@
 					const apiToken = process.env.CLOUDFLARE_API_TOKEN;
 					const accountId = ...;
+					const registryCredentialsClient =
+						apiToken && accountId
+							? createRegistryCredentialsClient({ accountId, apiToken })
+							: undefined;
 
 					if (hasCFRegistryImages) {
 						if (!apiToken || !accountId) throw ...;
-						configureOpenAPIForContainerPull(accountId, apiToken);
 					}
@@
 						logger: viteDevServer.config.logger,
+						registryCredentialsClient,
 					});
+
+diff --git a/packages/vite-plugin-cloudflare/src/plugins/preview.ts b/packages/vite-plugin-cloudflare/src/plugins/preview.ts
+--- a/packages/vite-plugin-cloudflare/src/plugins/preview.ts
++++ b/packages/vite-plugin-cloudflare/src/plugins/preview.ts
+@@
+-	configureOpenAPIForContainerPull,
++	createRegistryCredentialsClient,
+@@
++			const registryCredentialsClient =
++				apiToken && accountId
++					? createRegistryCredentialsClient({ accountId, apiToken })
++					: undefined;
+@@
+-				configureOpenAPIForContainerPull(accountId, apiToken);
+@@
+ 				logger: vitePreviewServer.config.logger,
++				registryCredentialsClient,
+ 			});
+
+# Review boundaries
+#
+# 1. The generated request options above should be factored rather than copied
+#    before production use.
+# 2. Existing Wrangler callers may continue using the static generated service
+#    during migration, but Vite must always pass an immutable operation client.
+# 3. External-only work without account/token should keep the existing warning
+#    and public-pull fallback without making any Cloudflare API request.
+# 4. Tests must prove token/account isolation under concurrency and must never
+#    snapshot or log bearer token values.
