diff --git a/packages/vite-plugin-cloudflare/src/context.ts b/packages/vite-plugin-cloudflare/src/context.ts
--- a/packages/vite-plugin-cloudflare/src/context.ts
+++ b/packages/vite-plugin-cloudflare/src/context.ts
@@ -27,8 +27,6 @@ export interface SharedContext {
 	workerNameToExportTypesMap?: Map<string, ExportTypes>;
 	hasShownWorkerConfigWarnings: boolean;
-	/** Tracks the number of in-flight dev server restarts (0 means no restart in progress) */
-	restartingDevServerCount: number;
 	/** Allowed hostnames for tunnel connections */
 	tunnelHostnames: Set<string>;
 }
@@ -41,6 +39,7 @@ export class PluginContext {
 	#sharedContext: SharedContext;
 	#resolvedPluginConfig?: ResolvedPluginConfig;
 	#resolvedViteConfig?: vite.ResolvedConfig;
+	#restartingDevServerCount = 0;
@@ -114,15 +113,19 @@ export class PluginContext {
 	}
 
 	beginRestartingDevServer(): void {
-		this.#sharedContext.restartingDevServerCount++;
+		this.#restartingDevServerCount++;
 	}
 
 	endRestartingDevServer(): void {
-		this.#sharedContext.restartingDevServerCount--;
+		assert(
+			this.#restartingDevServerCount > 0,
+			"Expected a matching dev-server restart"
+		);
+		this.#restartingDevServerCount--;
 	}
 
 	get isRestartingDevServer(): boolean {
-		return this.#sharedContext.restartingDevServerCount > 0;
+		return this.#restartingDevServerCount > 0;
 	}
 diff --git a/packages/vite-plugin-cloudflare/src/index.ts b/packages/vite-plugin-cloudflare/src/index.ts
--- a/packages/vite-plugin-cloudflare/src/index.ts
+++ b/packages/vite-plugin-cloudflare/src/index.ts
@@ -55,7 +55,6 @@ export type { WorkerConfig } from "./workers-configs";
 const sharedContext: SharedContext = {
 	hasShownWorkerConfigWarnings: false,
-	restartingDevServerCount: 0,
 	tunnelHostnames: new Set(),
 };
