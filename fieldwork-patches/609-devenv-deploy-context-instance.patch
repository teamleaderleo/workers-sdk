diff --git a/packages/wrangler/src/api/startDevWorker/DevEnv.ts b/packages/wrangler/src/api/startDevWorker/DevEnv.ts
--- a/packages/wrangler/src/api/startDevWorker/DevEnv.ts
+++ b/packages/wrangler/src/api/startDevWorker/DevEnv.ts
@@
-import { initDeployHelpersContext } from "@cloudflare/deploy-helpers/context";
+import { runWithDeployHelpersContext } from "@cloudflare/deploy-helpers";
@@
 export class DevEnv extends EventEmitter implements ControllerBus {
+	readonly #deployHelpersContext = {
+		logger,
+		fetchResult,
+		fetchListResult,
+		fetchPagedListResult,
+		fetchKVGetValue,
+		confirm,
+		prompt,
+		select,
+	};
+
 	config: ConfigController;
@@
 	async startWorker(options: WranglerStartDevWorkerInput): Promise<Worker> {
-		initDeployHelpersContext({
-			logger,
-			fetchResult,
-			fetchListResult,
-			fetchPagedListResult,
-			fetchKVGetValue,
-			confirm,
-			prompt,
-			select,
-		});
-
-		const worker = createWorkerObject(this);
-
-		try {
-			await this.config.set(options, true);
-		} catch (e) {
-			const error = new Error("An error occurred when starting the server", {
-				cause: e,
-			});
-			this.proxy.ready.reject(error);
-			await worker.dispose();
-			throw e;
-		}
-
-		return worker;
+		return runWithDeployHelpersContext(this.#deployHelpersContext, async () => {
+			const worker = createWorkerObject(this);
+
+			try {
+				await this.config.set(options, true);
+			} catch (e) {
+				const error = new Error("An error occurred when starting the server", {
+					cause: e,
+				});
+				this.proxy.ready.reject(error);
+				await worker.dispose();
+				throw e;
+			}
+
+			return worker;
+		});
 	}
@@
 	dispatch(event: ControllerEvent): void {
+		return runWithDeployHelpersContext(this.#deployHelpersContext, () =>
+			this.#dispatch(event)
+		);
+	}
+
+	#dispatch(event: ControllerEvent): void {
 		switch (event.type) {
@@
-	async teardown() {
+	async teardown() {
+		return runWithDeployHelpersContext(this.#deployHelpersContext, () =>
+			this.#teardown()
+		);
+	}
+
+	async #teardown() {
 		await runWithLogLevel(this.config.latestInput?.dev?.logLevel, async () => {

# This patch stacks on the operation-context API in workers-sdk PR #25.
# Required controls before promotion:
# - execute DevEnv.deploy-context.test.ts before and after the patch;
# - exercise bundleComplete and preview-token refresh timer paths;
# - reverse A/B event and completion order;
# - verify teardown and error dispatch remain owner-correct;
# - review any externally invoked callbacks that need registration-time binding
#   rather than invocation-time context.
